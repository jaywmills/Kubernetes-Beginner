apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
spec:
  containers:
  - name: nginx
    image: nginx

# Used the “kubectl create -f pod.yaml” and we return a value of “pod/nginx created”.
